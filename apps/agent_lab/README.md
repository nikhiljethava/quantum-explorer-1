# Agent Lab

This is an isolated environment for testing and debugging ADK agents without needing the full product UI or infrastructure.

## Usage
Run the following script to start the ADK interface interacting with the `packages/agents` directory:

```bash
./run_adk_web.sh
```
